/**
 * Reseller API Documentation
 * For Resellers. <br> Who are looking to Innovate with Ingram Micro's API SolutionsAutomate your eCommerce with our offering of APIs and Webhooks to create a seamless experience for your customers.
 *
 * The version of the OpenAPI document: 6.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The FreightResponseFreightEstimateResponseLinesInner model module.
 * @module model/FreightResponseFreightEstimateResponseLinesInner
 * @version 6.0
 */
class FreightResponseFreightEstimateResponseLinesInner {
    /**
     * Constructs a new <code>FreightResponseFreightEstimateResponseLinesInner</code>.
     * @alias module:model/FreightResponseFreightEstimateResponseLinesInner
     */
    constructor() { 
        
        FreightResponseFreightEstimateResponseLinesInner.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>FreightResponseFreightEstimateResponseLinesInner</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FreightResponseFreightEstimateResponseLinesInner} obj Optional instance to populate.
     * @return {module:model/FreightResponseFreightEstimateResponseLinesInner} The populated <code>FreightResponseFreightEstimateResponseLinesInner</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FreightResponseFreightEstimateResponseLinesInner();

            if (data.hasOwnProperty('ingramPartNumber')) {
                obj['ingramPartNumber'] = ApiClient.convertToType(data['ingramPartNumber'], 'String');
            }
            if (data.hasOwnProperty('vendorPartNumber')) {
                obj['vendorPartNumber'] = ApiClient.convertToType(data['vendorPartNumber'], 'String');
            }
            if (data.hasOwnProperty('warehouseId')) {
                obj['warehouseId'] = ApiClient.convertToType(data['warehouseId'], 'String');
            }
            if (data.hasOwnProperty('quantity')) {
                obj['quantity'] = ApiClient.convertToType(data['quantity'], 'Number');
            }
            if (data.hasOwnProperty('unitPrice')) {
                obj['unitPrice'] = ApiClient.convertToType(data['unitPrice'], 'Number');
            }
            if (data.hasOwnProperty('netAmount')) {
                obj['netAmount'] = ApiClient.convertToType(data['netAmount'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FreightResponseFreightEstimateResponseLinesInner</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FreightResponseFreightEstimateResponseLinesInner</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['ingramPartNumber'] && !(typeof data['ingramPartNumber'] === 'string' || data['ingramPartNumber'] instanceof String)) {
            throw new Error("Expected the field `ingramPartNumber` to be a primitive type in the JSON string but got " + data['ingramPartNumber']);
        }
        // ensure the json data is a string
        if (data['vendorPartNumber'] && !(typeof data['vendorPartNumber'] === 'string' || data['vendorPartNumber'] instanceof String)) {
            throw new Error("Expected the field `vendorPartNumber` to be a primitive type in the JSON string but got " + data['vendorPartNumber']);
        }
        // ensure the json data is a string
        if (data['warehouseId'] && !(typeof data['warehouseId'] === 'string' || data['warehouseId'] instanceof String)) {
            throw new Error("Expected the field `warehouseId` to be a primitive type in the JSON string but got " + data['warehouseId']);
        }

        return true;
    }


}



/**
 * The unique IngramMicro part number.
 * @member {String} ingramPartNumber
 */
FreightResponseFreightEstimateResponseLinesInner.prototype['ingramPartNumber'] = undefined;

/**
 * The vendor part number for the line item.
 * @member {String} vendorPartNumber
 */
FreightResponseFreightEstimateResponseLinesInner.prototype['vendorPartNumber'] = undefined;

/**
 * The ID of the warehouse the line item will ship from.
 * @member {String} warehouseId
 */
FreightResponseFreightEstimateResponseLinesInner.prototype['warehouseId'] = undefined;

/**
 * The requested quantity of the line item.
 * @member {Number} quantity
 */
FreightResponseFreightEstimateResponseLinesInner.prototype['quantity'] = undefined;

/**
 * The unit price for the line item.
 * @member {Number} unitPrice
 */
FreightResponseFreightEstimateResponseLinesInner.prototype['unitPrice'] = undefined;

/**
 * The net amount (unit price X quantity) for the line item.
 * @member {Number} netAmount
 */
FreightResponseFreightEstimateResponseLinesInner.prototype['netAmount'] = undefined;






export default FreightResponseFreightEstimateResponseLinesInner;

